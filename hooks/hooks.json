{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "bellwether hooks check --format json",
            "timeout": 15
          },
          {
            "type": "command",
            "if": "Bash(gh pr create*)",
            "command": "bellwether hooks check --format json",
            "timeout": 15
          },
          {
            "type": "command",
            "if": "Bash(gh pr ready*)",
            "command": "bellwether hooks check --format json",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
